---
name: sw-simple-change
description: 执行已确认不需要完整任务文档的局部代码或文档变更，并完成审查、验证、提交确认和迭代变更记录。仅在 route-task 判定为简单任务后使用。
---

# 执行简单变更

## 上下文契约

必读：`AGENTS.md`、`AGENTS.local.md`、用户请求、目标仓库文档。

按需读取：相关任务（若用户选择同步）、目标模块代码、测试、`project/repositories/<repo-id>.md`、`AGENTS.md` 规范路由命中的文档。

初始禁止：无关任务、全部项目知识、其他仓库源码、`verification.md` 工作流模板。

输出：最小代码修改、相关验证结果、code-review 结论，以及经确认后的提交和 `changes.jsonl` 记录。

## 流程

1. 确认目标仓库、局部范围、验证方式和记录变更的开放迭代，并按 `AGENTS.md` 读取直接相关规范；多个迭代时让用户选择。完成：仍满足简单任务全部硬门禁，否则转工作流任务。
2. 记录 branch、HEAD 和脏文件；重叠修改交由用户决定，不重叠修改经确认后排除。完成：提交与审查基线明确。
3. 复用目标仓库现有模式完成最小修改，并即时同步失真的项目文档。可隔离的新逻辑和 Bug 优先先写失败测试；纯 UI 或无测试接缝的遗留代码使用最小替代验证。完成：修改和直接相关验证通过。
4. 调用 `sw-code-review`，处理阻塞项并复审。完成：P0 已解决或证明误报，P1 已解决或由用户明确接受；自动验证无法覆盖时只列出必要的业务确认。
5. 汇总一次提交计划并取得一次确认；不得 push 或 merge。完成：代码提交成功后，`change add` 已把摘要、真实 commit、验证和项目文档追加到所选迭代的 `changes.jsonl`，工作流记录已提交。

## 审查处置

P0 阻塞提交，但不授权自动修改。P1 可由用户明确接受风险；P2/P3 不阻塞。仅可自动修复任务自有、来源明确、局部可逆且不改变行为的问题；密钥、权限、数据、公共契约和来源不明修改必须由用户决定。

---
name: sw-prd
description: 澄清原始需求并创建或更新任务的当前 PRD 与决策记录；存在必须由用户决定的未决项时调用 grilling。新工作流任务、需求变化、验收口径不清、需求需要渐进探索或用户要求整理需求时使用。
---

# 形成当前 PRD

## 上下文契约

必读：当前任务的 `task.json`、用户原始需求，以及已有的 `prd.md`、`decisions.md`（若存在）。

按需读取：`AGENTS.md` 规范路由命中的文档、直接相关的项目索引、仓库说明、一个已有任务 PRD、为回答事实问题所需的少量源码或文档；UI 还原任务中只读取用户已选定且与当前问题直接相关的 Figma 节点或设计截图。

初始禁止：技术方案、实施方案、全部项目知识、无关任务、全仓库扫描，以及未选定范围的完整 Figma 文件、组件库和无关设计页面。

输出：保持当前事实和开放问题的 `prd.md`、追加式 `decisions.md`、必要的项目长期记忆更新，以及待用户确认的需求摘要。

## 流程

1. 运行 `task status <task> --json`，确认任务仍在 `prd`；整理用户原始需求、已有 PRD、决策记录和直接相关的项目事实。UI 还原任务只确认纳入范围的页面、状态、目标视口、响应式范围和精确设计定位器；只有文件级 Figma 链接且无法确定有效 frame 时请用户选择，不读取整份设计稿猜测范围。完成：访谈主题、已知事实和当前需求变化已明确。
2. 判断澄清路径。目标、范围和验收口径已有充分事实时直接整理 PRD；存在少量必须由用户决定的未决项时调用一次 `grilling`，并让待决范围和完成标准覆盖实际缺口。问题需要设计证据时只读取与当前问题对应的精确节点，把事实摘要而非原始 Figma 响应交给 `grilling`；问题互相依赖、无法在当前上下文完整表述时使用下述渐进探索。完成：选择了与需求不确定性匹配的路径，没有为清晰需求安排访谈，也没有替用户决定真实取舍。
3. 创建或更新唯一的 `prd.md` 和追加式 `decisions.md`，不因惯例补造业务事实；同步本轮已经确认的长期记忆变化。在“约束与依赖”中用 inline code 写出本阶段实际读取的 `CONTEXT.md`、`adr/*.md` 或 `project/*.md` 根相对路径。完成：文档反映当前需求和开放问题，长期记忆与已确认结论一致。
4. 所有开放问题收敛后补齐完整 PRD 和稳定 AC ID，将“未决问题”写为“无”，并在当前任务的 PRD 与 decisions 中检查已解决 Q ID 不再被描述为待决条件。自行检查目标、范围、规则、异常和 AC 是否一致并向用户展示关键结论。只有用户明确确认后，才进入 `technical_design` 并调用 `sw-technical-design`。

## 渐进探索

1. 先调用 `grilling` 确认本任务的目标和非目标，再广度优先识别当前能够准确表述的问题。不要沿单一分支提前深入，也不要为尚无法准确提问的区域编造问题。
2. 在 `prd.md` 的“未决问题”中维护三个三级标题：
   - “当前前沿”：已经准确表述、没有未解决依赖且现在可以处理的问题。
   - “后续问题”：已经准确表述但仍被其他问题阻塞的问题；写出阻塞它的 Q ID。
   - “尚未明确”：仍在范围内、但依赖前序答案才能准确提问的区域。
3. 为准确表述的问题分配稳定 Q ID，从 `Q-001` 开始；每项写明问题、期望得到的答案和阻塞关系。尚未明确的区域不分配 Q ID。
4. 每次只处理当前前沿中一个可独立收敛的问题。事实问题自行读取必要证据；必须由用户判断的问题调用 `grilling`，并把待决范围和完成标准限定为该问题。遇到含糊、一词多义或与 `CONTEXT.md` 冲突的表达时调用 `sw-domain-modeling`。
5. 每解决一个问题，立即把决定、理由和影响连同 Q ID 追加到 `decisions.md`，把结论同步进 PRD 正文，并移除正文中把该 Q 视为未解决前提的旧表述，然后重新计算当前前沿、后续问题和尚未明确。不要等整个探索结束后批量保存。
6. 当前前沿为空但仍有后续问题或尚未明确时，记录实际阻塞并停止；不得代替用户作答或把未知内容写成默认假设。

## 文档规则

按下述文档规则创建并维护一份当前 `prd.md`。将原始需求与 grilling 结论合并成可读文本；需求变化时直接更新，历史交给 Git。探索状态只保存在“未决问题”中，不创建独立 map、ticket 或阶段；已解决问题的详细答案只保存在 `decisions.md`，PRD 正文只保留其形成的当前需求事实。

为每条验收标准分配稳定 ID，从 `AC-001` 开始。优先使用三级标题并写出可观察结果，例如：

```markdown
### AC-001 <可验证行为>

<前置条件、动作和预期结果>
```

不要在后续阶段重编号已确认 ID；删除或替代时在 `decisions.md` 记录原因。

UI 还原需求不把完整设计属性复制进 PRD。正文只保存实际范围所需的设计 URL 或 `fileKey + nodeId`、节点名称、设计版本（可获得时）、状态、目标视口、响应式范围和来源优先级；Figma 负责指定视口下的视觉事实，PRD 仍需明确设计稿无法表达的业务行为、异常、权限和无障碍要求。视觉 AC 不得只写“与 Figma 一致”，应能定位对应节点、状态和视口，但不为实施阶段抄录像素值。

## 决策记录

仅把影响范围、行为、兼容性或验收的关键选择追加到 `decisions.md`。每项写日期、关联 Q ID（若有）、决定、理由、影响；被替代时追加“取代哪项”，不改写旧记录。任务决定只有通过 `sw-domain-modeling` 的 ADR 三项门槛并取得用户确认后，才提升为项目级关键决策。

阶段命令：`task phase <task> technical_design --confirmed`。
